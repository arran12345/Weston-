# Copy this file to .env before first run:
#   cp .env.example .env
#
# The SQLite database is a single file on disk. Back it up by copying it
# (PRD Section 17); nothing else is needed.
DATABASE_URL="file:./prisma/dev.db"
